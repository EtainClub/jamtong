# 서비스 계정 키. 이 파일이 공개 저장소에 한 번 들어가면 커밋을 지워도
# 이미 유출된 것으로 봐야 한다 — 되돌리는 방법은 키 폐기뿐이다.
sa.json
service-account*.json
*-service-account.json
*serviceaccount*.json
*.key.json

/node_modules
/.pnp
.pnp.*
.yarn/*

/coverage

/.next/
/out/
/build

.DS_Store
*.pem

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

.env*
!.env.example

/.vercel
*.tsbuildinfo
next-env.d.ts
.claude/

# 자료 조사용 스캔·촬영본. 남의 책을 찍은 사진이므로 공개 저장소에 올리지 않는다.
# 여기서 뽑아낸 것은 '무엇을 찾아볼지'의 목록이고, 인용은 1차 자료로만 한다.
/refs/
.refs-preview/
